## LLM serving workload lifecycle management

For a developer-facing walkthrough of the current `ModelServing` controller
reconcile sequence, rolling update behavior, recovery paths, status calculation,
and plugin hook flow, see [RECONCILE_FLOW.md](./RECONCILE_FLOW.md).
